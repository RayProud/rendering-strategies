{
  "name": "rendering-next",
  "description": "Frontend monorepo with different the same next.js hello world app with different adapter each",
  "version": "0.1.0",
  "private": true,
  "workspaces": {
    "packages": [
      "applications/*",
      "packages/*"
    ]
  },
  "packageManager": "yarn@4.9.2",
  "engines": {
    "node": "20.x"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
